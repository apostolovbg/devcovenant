# Core Policies
**Last Updated:** 2026-02-20
**Version:** 0.2.6

## Table of Contents
- [Overview](#overview)
- [Layout Contract](#layout-contract)
- [Descriptor Contract](#descriptor-contract)
- [Policy Runtime Contract](#policy-runtime-contract)
- [Fixer Contract](#fixer-contract)
- [Workflow](#workflow)

## Overview
Core policies are shipped under `devcovenant/core/policies/`.
Each policy directory defines one policy contract and optional fixers/assets.

Runtime behavior is sourced from AGENTS policy definitions generated by
refresh from policy descriptors plus metadata resolution layers.

## Layout Contract
Each policy directory uses this layout:
- `<policy-id>.yaml` descriptor
- `<policy-id>.py` runtime check script
- optional `fixers/*.py`
- optional `assets/` templates

The directory name and script/descriptor basename must match policy ID.

## Descriptor Contract
Descriptors define:
- `id`
- `text` policy prose
- `metadata` defaults

Resolved metadata is computed by runtime precedence and then written into:
- `devcovenant/registry/local/policy_registry.yaml`
- AGENTS managed policy block

Example metadata-owned runtime behavior:
- `devflow-run-gates.required_commands` is resolved from descriptor/profile/
  config layers and executed by `devcovenant test` in declared order.

## Policy Runtime Contract
Policy scripts expose policy checks and return structured violations consumed
by engine formatting and gate behavior.

Policies should remain language-agnostic. Language-aware analysis is delegated
to shared translator runtime, which returns normalized language units.

## Fixer Contract
Fixers are optional.
When present, they live in `fixers/` and follow language-aware fallback rules:
- language-specific fixer when available
- `fixers/global.py` fallback otherwise

A custom policy override suppresses core fixers for that policy ID.

## Workflow
1. Update descriptor and script together.
2. Update mirrored tests under `tests/devcovenant/core/policies/...`.
3. Keep tests behavioral/contract-focused; do not add placeholder stubs.
4. Run `devcovenant refresh` after descriptor metadata changes.
5. Run full gate sequence before finalizing.

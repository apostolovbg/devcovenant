version: 1
profile: php
category: language
suffixes:
- .php
ignore_dirs:
- vendor
assets:
- path: composer.json
  template: composer.json
  mode: replace
policy_overlays:
  dependency-license-sync:
    dependency_files:
    - composer.json

# DevCovenant Configuration
# Selector metadata accepts comma-separated strings or YAML lists (see
# devcovenant/README.md for the canonical selector keys). Use the same
# names here when overriding policy defaults.

# DevCovenant core exclusion guard. For user repos, keep `devcov_core_include`
# set to false so core files stay out of enforcement and remain update-safe.
# Do not edit the core path list in user repos; only the DevCovenant repo
# should enable core inclusion.
devcov_core_include: true
devcov_core_paths:
  - devcovenant/core
  - devcovenant/__init__.py
  - devcovenant/__main__.py
  - devcovenant/cli.py
  - tools/run_pre_commit.py
  - tools/run_tests.py
  - tools/update_test_status.py

profiles:
  active:
    - python
    - docs
    - data
  generated:
    file_suffixes:
      - __none__

paths:
  policy_definitions: AGENTS.md
  registry_file: devcovenant/registry.json
  manifest_file: devcovenant/manifest.json
  templates_root: devcovenant/core/templates
  custom_templates_root: devcovenant/custom/templates

docs:
  managed_blocks:
    begin: "<!-- DEVCOV:BEGIN -->"
    end: "<!-- DEVCOV:END -->"
  optional:
    spec: true
    plan: true

install:
  include_spec: true
  include_plan: true
  allow_existing: false
  auto_uninstall: false
  preserve_custom: true
  config_mode: preserve
  doc_mode: preserve
  metadata_mode: preserve
  disable_policy: []

update:
  preserve_custom: true
  refresh_core: true

engine:
  master_update: true
  fix_threshold: warning
  fail_threshold: warning
  auto_fix_enabled: true
  parallel_checks: true
  verbose: true
  pre_commit_all_files: true
  file_suffixes:
    - .md
    - .rst
    - .txt
    - .yml
    - .yaml
    - .json
    - .toml
  ignore_dirs:
    - .git
    - .venv
    - .python
    - output
    - logs
    - build
    - dist
    - node_modules
    - __pycache__
    - .pytest_cache
    - .cache
    - .venv.lock

self_enforcement:
  enabled: true
  policy_prefix: "devcov-"

hooks:
  pre_commit: true
  pre_push: false

reporting:
  show_policy_links: true
  audit_trail: true
  use_colors: true

ignore:
  patterns: []

policies:
  managed-environment:
    apply: false
    expected_paths: []
    expected_interpreters: []
    required_commands: []
    command_hints: []

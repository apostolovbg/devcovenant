# Root workspace dependencies for DevCovenant development and release work.
# Keep the shipped DevCovenant runtime reference so source/CI execution uses
# the same runtime surface as installed repos.
-r devcovenant/runtime-requirements.lock

# Add repository-only workspace dependencies below.
bandit==1.9.4
cyclonedx-bom==7.2.2
pip-audit==2.10.0
pipx==1.11.1
twine==6.2.0
